250416
1교시
python 오버라이딩 방법 다른것 배움
함수에 매개변수로 넣으면 왜 오버라이딩이 안되는지 ** 기술변접으로 물어볼 수 있다.**
vscode 다운 및 extesion 다운 (korean, python 다운. python 다운시 python 전용 오타 등 확인가능)

import 시 _pycache_ 가 다운된다. 첫 import때 생성되어 실행할때 속도가 느리다.

2교시
** 함수.py를 생성 후 if __name__ == "__main__" 을 사용하면 호출할때만 실행되게 함.

함수 안에 있는 변수들은 함수 밖으로 호출하지 못함. (* if, for 등의 변수는 접근 가능)

- 전역변수, 지역변수 * 렉시컬 특성 *
함수에서 전역변수를 사용할 땐 global 전역변수를 호출 해야한다.
만약 global 전역변수 호출을 하지 않으면 함수안에 지역변수로 새로 생성된다. (변수의 우선순위)

함수에서 무조건 변수 생성이 먼저 된 후
함수가 다시 위에서 부터 실행된다.
그래서 변수의 값이 할당이 안되어 있어 오류가 난다.

3교시
함수에서 변수를 호출 시 함수안의 지역변수로 새로 생성된다. 함수호출 시 사용한 변수가 아닌
함수 내의 지역변수로 새로 생성되어 주소값 또한 변경이 된다.
call by value
call by reference

4교시
return. return은 기본적으로 1개 반환하지만 return a,b는 자동으로 tuple로 감싸져서 return됨.
- 매개변수
	1. default 값이 있는 매개변수는 항상 순서인수 뒤에 와야 함.
	2. 함수 호출 시 매개변수의 변수명을 입력하면 순서와 상관없이 호출 할 수 있다.


5교시
	3. 튜플 매개변수 : 매개변수가 가변 개수가 됨.
	4. 딕셔너리 매개변수 : 키워드 인수를 딕셔너리 형태로 받아 가변 키워드 인수가 됨.
		ex)name='홍길동', age=30

6교시
	함수 정의시 매개변수 순서
	순서인수(기본) > 튜플 매개변수(*arg) > 키워드인수(sep='/') > 딕셔너리 매개변수(**arg)

7교시
튜플인수 언패킹 - 매개변수로 튜플을 받을 경우 *tuple_arg 이렇게 * 추가하면 언패킹됨.
딕셔너리 언패킹 - 매개변수로 딕셔너리 받을 경우 **dict_arg 이렇게 ** 추가하면 언패킹됨.

8교시
람다 형태 = lambda 매개변수 : return 값
if 조건문 = return값 if 조건식 else return값

python 코드 길땐 (역슬레쉬) 입력 후 enter 치면 됨

